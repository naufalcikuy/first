
<!DOCTYPE html>
{% extends 'kelola/base.html' %}
{% load staticfiles %}

<html lang="en">
<head>
    <title>Kelola Pengetahuan</title>
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap-theme.min.css">
    <link rel="stylesheet" href="{% static 'css/kelola.css' %}">
    <link href="//fonts.googleapis.com/css?family=Lobster&subset=latin,latin-ext" rel="stylesheet" type="text/css">
    <!--<meta charset="UTF-8">-->

</head>
<body>
<div class='page-header'>
    <h1><a href="/">Kelola Pengetahuan</a></h1>
</div>

<div class="content container">
<div class="row">
<div class="col-md-8">

{% block content %}
HALAMAN KELOLA PENGETAHUAN
<div>
		<a href="{% url 'post_new' %}" class="btn btn-default">Tambah Pengetahuan</a>
		
</div>

<div>
		<table class="table">
			<tr>
		    	<th>Category</th>
		    	<th>Title</th> 
		    	<th>Text</th>
		    	<th>Action</th>
		  	</tr>
			{% for item in knowledges_health %}
			<tr>
			    <td>{{ item.category }} </td>
			    <td>{{ item.title }}</td> 
			    <td>{{ item.text }}</td>
			    <td> <a class=" btn btn-warning" href = "{% url 'edit_knowledge' item.id %}">Edit</a>
			    	 <button type="button" class= "btn btn-danger" data-toggle="modal" data-target="#myModal{{item.id}}"><span class="glyphicon glyphicon-trash"></span></button>
			    </td>
			</tr>
			{% endfor %}  
		</table>
</div>

<div>
		<table class="table">
			<tr>
		    	<th>Category</th>
		    	<th>Title</th> 
		    	<th>Text</th>
		    	<th>Action</th>
		  	</tr>
			{% for item in knowledges_growing %}
			<tr>
			    <td>{{ item.category }} </td>
			    <td>{{ item.title }}</td> 
			    <td>{{ item.text }}</td>
			    <td> <a class=" btn btn-warning" href = "{% url 'edit_knowledge' item.id %}">Edit</a>
			    	 <button type="button" class= "btn btn-danger" data-toggle="modal" data-target="#myModal{{item.id}}"><span class="glyphicon glyphicon-trash"></span></button>
			    </td>
			</tr>
			{% endfor %}  
		</table>
</div>
		<table class="table">
			<tr>
		    	<th>Category</th>
		    	<th>Title</th> 
		    	<th>Text</th>
		    	<th>Action</th>
		  	</tr>
			{% for item in knowledges_development %}
			<tr>
			    <td>{{ item.category }} </td>
			    <td>{{ item.title }}</td> 
			    <td>{{ item.text }}</td>
			    <td> <a class=" btn btn-warning" href = "{% url 'edit_knowledge' item.id %}">Edit</a>
			    	 <button type="button" class= "btn btn-danger" data-toggle="modal" data-target="#myModal{{item.id}}"><span class="glyphicon glyphicon-trash"></span></button>
			    </td>
			</tr>
			{% endfor %}  
		</table>
<div>
		<table class="table">
			<tr>
		    	<th>Category</th>
		    	<th>Title</th> 
		    	<th>Text</th>
		    	<th>Action</th>
		  	</tr>
			{% for item in knowledges_immunization %}
			<tr>
			    <td>{{ item.category }} </td>
			    <td>{{ item.title }}</td> 
			    <td>{{ item.text|slice:":100" }}</td>
			    <td> <a class=" btn btn-warning" href = "{% url 'edit_knowledge' item.id %}">Edit</a>
			    	 <button type="button" class= "btn btn-danger" data-toggle="modal" data-target="#myModal{{item.id}}"><span class="glyphicon glyphicon-trash"></span></button>
			    </td>
			</tr>
			{% endfor %}  
		</table>
</div>	


{% for item in knowledges %}

   		 <!-- Trigger the modal with a button -->
			

			<!-- Modal -->
			<div id="myModal{{item.id}}" class="modal fade" role="dialog">
			  <div class="modal-dialog">

			    <!-- Modal content-->
			    <div class="modal-content">
			      <div class="modal-header">
			        <button type="button" class="close" data-dismiss="modal">&times;</button>
			        <h4 class="modal-title">Modal Header</h4>
			      </div>
			      <div class="modal-body">
			        <p> Apakah anda yakin menghapus pengetahuan untuk
								<h1>id: {{item.id}}</h1>
								<h1>title: {{item.title}}</h1>
								<h1>text: {{item.text}}</h1></p>
			      </div>
			      <div class="modal-footer">
				      	<a href="{% url 'delete_knowledge' item.id %}"><button type="button" class="btn btn-danger">Delete</button></a>
								<button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
			       
			      </div>
			    </div>

			  </div>
			</div>
		{% endfor %}  


{% endblock %}
</div>
</div>
</div>


</body>
</html>


